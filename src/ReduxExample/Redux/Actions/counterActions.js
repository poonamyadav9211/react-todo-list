import {INCREMENT, DECREMENT} from '../Actions/type';

export const increment = () => (
    {
        type: INCREMENT    
    }
);

export const decrement = () => (
    {
        type: DECREMENT
    }
);

